<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="MediaDaw - Tu tienda de equipamiento de producción musical">
    <meta name="author" content="MediaDaw Team">

    {# CSRF Token para peticiones AJAX (Spring Security) #}
    <meta name="_csrf" content="{{ _csrf.token }}">
    <meta name="_csrf_header" content="{{ _csrf.headerName }}">

    <title>{% block title %}MediaDaw - Tu tienda de producción musical{% endblock %}</title>

    {# Bootstrap 5.3.2 CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    {# Bootstrap Icons #}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    {# Estilos personalizados de MediaDaw #}
    <link rel="stylesheet" href="/css/mediadaw.css">

    {# Bloque para CSS adicionales en vistas específicas #}
    {% block extra_css %}{% endblock %}

    <style>
        /* --- PERSONALIZACIÓN DE MARCA MEDIAMARKT STYLE --- */
        :root {
            --mm-red: #DF0000;
            --mm-dark-red: #b30000;
            --mm-black: #191919;
            --mm-gray-bg: #F4F4F4;
            --font-display: 'Arial Black', 'Impact', sans-serif;
        }

        body {
            background-color: var(--mm-gray-bg);
            color: var(--mm-black);
            font-family: 'Arial', sans-serif;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        main {
            flex: 1;
        }

        /* Header Rojo Personalizado */
        .bg-brand-red {
            background-color: var(--mm-red) !important;
        }

        /* Logo Inclinado */
        .logo-img {
            max-height: 50px;
        }

        /* Buscador: Botón negro */
        .btn-search {
            background-color: var(--mm-black);
            color: white;
            font-weight: bold;
            border: none;
        }
        .btn-search:hover {
            background-color: #333;
            color: white;
        }

        /* Hero Section con gradiente */
        .hero-section {
            background-color: var(--mm-black);
            background-image: linear-gradient(45deg, #191919 25%, #222 25%, #222 50%, #191919 50%, #191919 75%, #222 75%, #222 100%);
            background-size: 20px 20px;
            color: white;
        }

        .hero-title {
            font-family: var(--font-display);
            color: var(--mm-red);
            transform: skewX(-5deg);
            font-size: 3rem;
        }

        /* Títulos de sección */
        .section-title {
            font-family: var(--font-display);
            border-left: 8px solid var(--mm-red);
            padding-left: 15px;
        }

        /* Cards de Producto */
        .productos-card {
            transition: transform 0.2s, box-shadow 0.2s;
            border: 1px solid #e5e5e5;
        }

        .productos-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.15);
            border-color: var(--mm-red);
        }

        .productos-img-placeholder {
            height: 180px;
            background-color: #eee;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #aaa;
            font-weight: bold;
        }

        /* Precio gigante */
        .price-tag {
            font-family: var(--font-display);
            font-size: 2.5rem;
            color: var(--mm-red);
            line-height: 1;
            letter-spacing: -1px;
        }

        .price-currency {
            font-size: 1rem;
            vertical-align: super;
            color: var(--mm-black);
        }

        /* Botón de compra */
        .btn-brand {
            background-color: var(--mm-red);
            color: white;
            font-weight: bold;
            border: none;
        }

        .btn-brand:hover {
            background-color: var(--mm-dark-red);
            color: white;
        }

        /* Footer */
        .footer-brand {
            background-color: var(--mm-black);
            color: white;
        }
        .footer-slogan {
            font-family: var(--font-display);
            color: var(--mm-red);
        }

        /* Badges */
        .custom-badge {
            background-color: #FFCC00;
            color: black;
            font-weight: bold;
            position: absolute;
            top: 10px;
            left: 10px;
            z-index: 10;
        }

        /* Estilos adicionales para product-card (compatibilidad) */
        .product-card {
            transition: transform 0.2s, box-shadow 0.2s;
            border: 1px solid #e5e5e5;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.15);
            border-color: var(--mm-red);
        }

        /* Estilos para tarjetas de categorías */
        .category-card {
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .category-card:hover {
            transform: translateY(-5px);
            border-color: var(--mm-red);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1) !important;
        }

        .category-card:hover i {
            transform: scale(1.2);
            transition: transform 0.3s ease;
        }
    </style>
</head>
<body>
    {# Header con Navbar #}
    {% include 'fragments/header' %}

    {# Componente de mensajes (alertas de éxito/error) #}
    {% include 'fragments/messages' %}

    {# Contenido principal de cada vista #}
    <main>
        {% block content %}
        {# El contenido específico de cada página va aquí #}
        {% endblock %}
    </main>

    {# Footer #}
    {% include 'fragments/footer' %}

    {# Bootstrap 5.3.2 JS Bundle (incluye Popper) #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

    {# Script para configurar CSRF en AJAX (jQuery o Fetch API) #}
    <script>
        // Configuración global de CSRF para peticiones AJAX
        const csrfToken = document.querySelector('meta[name="_csrf"]').getAttribute('content');
        const csrfHeader = document.querySelector('meta[name="_csrf_header"]').getAttribute('content');

        // Para Fetch API
        const defaultHeaders = {
            [csrfHeader]: csrfToken,
            'Content-Type': 'application/json'
        };
    </script>

    {# Bloque para JavaScript adicionales en vistas específicas #}
    {% block extra_js %}{% endblock %}
</body>
</html>
